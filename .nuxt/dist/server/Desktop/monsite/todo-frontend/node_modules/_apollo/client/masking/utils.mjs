import { Slot } from "optimism";
import { invariant } from "../utilities/globals/invariantWrappers.mjs";
import "../utilities/globals/global.mjs";
import { canUseWeakSet, canUseWeakMap } from "../utilities/common/canUse.mjs";
var MapImpl = canUseWeakMap ? WeakMap : Map;
var SetImpl = canUseWeakSet ? WeakSet : Set;
var disableWarningsSlot = new Slot();
var issuedWarning = false;
function warnOnImproperCacheImplementation() {
  if (!issuedWarning) {
    issuedWarning = true;
    globalThis.__DEV__ !== false && invariant.warn(52);
  }
}
export {
  MapImpl,
  SetImpl,
  disableWarningsSlot,
  warnOnImproperCacheImplementation
};
//# sourceMappingURL=utils.mjs.map
